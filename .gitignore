# Maven
target/
*.class

# Performance test results (generated — not committed)
perf-results/
*.jtl
*.log

# Keep these — essential for performance pipeline
# openapi.yaml        ← spec sent to perf agent per run
# .perf_spec_hash     ← tracks last JMX generation hash
# perf-config.yaml    ← team perf configuration

# IDE
.idea/
*.iml
.vscode/
